/**
 * Barrel export para todos os hooks de movimentações
 */

// Queries (leitura)
export { useMovimentacoes, movimentacoesKeys } from './queries';

// Mutations (escrita) – futuro
// export { useCreateMovimentacao, useUpdateMovimentacao, useDeleteMovimentacao } from './mutations';
